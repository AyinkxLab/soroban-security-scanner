# Examples

Runnable examples demonstrating the scanner.

Once the scanner is functional, this directory will contain:

- A minimal Soroban project with intentional (documented) vulnerabilities.
- A clean Soroban project with no findings.
- Example configuration files.
- Example SARIF and JSON outputs.

Examples must be safe to run and must never require network access.
