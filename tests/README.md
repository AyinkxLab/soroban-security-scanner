# Integration Tests

Crate-level integration tests live in each crate's `tests/` directory.
Files in this top-level directory are reserved for cross-crate, end-to-end
tests that exercise the CLI as a black box.

Current tests are minimal; the end-to-end suite is built out in Phases 6–7.
